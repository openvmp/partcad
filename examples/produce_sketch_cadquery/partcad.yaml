desc: This example demonstrates how to define a sketch using a CadQuery script.

docs:
  usage: |
    ```shell
    pc inspect -s sketch
    ```

sketches:
  sketch:
    type: cadquery
    desc: A sample sketch

render:
  readme:
  svg:
